import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonText: {
        color: "#fff",
        textAlign: "center"
    },
    buttonStyle: {
        padding: 10,
        backgroundColor: "#000",
        borderRadius: 4
    }
});