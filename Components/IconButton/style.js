import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    iconText: {
        fontWeight: "100",
        fontSize: 14,
        textAlign: "center",
        color: "#747373"
    },
    icon: {
        alignContent: "center",
        justifyContent: "center",
        textAlign: "center"
    }
});